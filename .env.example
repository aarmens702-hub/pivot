# Frontend env vars (Vite). Copy to `.env.local` for local dev or set in
# Vercel for production.

# Base URL for the Pivot backend API. Leave blank in dev (Vite proxy handles
# /api → http://127.0.0.1:5174). Set to the deployed Railway URL in prod.
VITE_API_BASE=
